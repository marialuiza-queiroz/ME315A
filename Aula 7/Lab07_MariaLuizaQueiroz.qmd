---
title: "ME315 - Lab 07"
author: "Maria Luiza dos Santos de Queiroz - RA: 183314"
format: pdf
editor: visual
---

# SQLite - Parte II

1.  Baixe o arquivo `uwmadison.sqlite3`. Conecte-se a ele usando o pacote `RSQLite`, armazenando a conexão em uma variável `conn`.

```{r}
library(DBI)
library(RSQLite)

conn <- dbConnect(SQLite(), "database.sqlite3")
```

2.  Quem são e quantos são os professores que lecionaram disciplinas cujo tópico era estatística (`subjects.abbreviation='STAT'`);

```{r}
profs <- dbGetQuery(conn, "
           SELECT DISTINCT instructors.name FROM instructors
           INNER JOIN teachings ON teachings.instructor_id = instructors.id
           INNER JOIN sections ON sections.uuid = teachings.section_uuid
           INNER JOIN subject_memberships ON 
           subject_memberships.course_offering_uuid 
           = sections.course_offering_uuid
           INNER JOIN subjects ON subjects.code 
           = subject_memberships.subject_code
           WHERE subjects.abbreviation = 'STAT'
           ")

profs

nrow(profs) #número de professores
```

3.  O GPA americano é definido numa escala de 0 a 4, em que A = 4, AB = 3.5, B = 3, BC = 2.5, C = 2, D = 1 e F = 0. Determinando a nota média de cada oferecimento pela ponderação da quantidade de alunos em cada extrato com os valores numéricos de cada conceito, indique (no que se referente a disciplinas no assunto de estatística):

- Quem é o professor mais difícil?

- Quem é o professor mais fácil?

- Qual é a disciplina mais difícil?

- Qual é a disciplina mais fácil?

```{r}
#Professores

professores <- dbGetQuery(conn, "
    SELECT instructors.name,
           SUM(4*a_count + 3.5*ab_count + 3*b_count + 2.5*bc_count + 2*c_count 
           + 1*d_count + 0*f_count) AS pontos,
           SUM(a_count+ab_count+b_count+bc_count+c_count+d_count+f_count) 
           AS alunos
    FROM grade_distributions
    INNER JOIN sections ON sections.course_offering_uuid 
    = grade_distributions.course_offering_uuid
                        AND sections.number 
                        = grade_distributions.section_number
    INNER JOIN teachings ON teachings.section_uuid = sections.uuid
    INNER JOIN instructors ON instructors.id = teachings.instructor_id
    INNER JOIN subject_memberships ON subject_memberships.course_offering_uuid 
    = sections.course_offering_uuid
    INNER JOIN subjects ON subjects.code = subject_memberships.subject_code
    WHERE subjects.abbreviation = 'STAT'
    GROUP BY instructors.name
")
professores$gpa <- professores$pontos / professores$alunos

professores[which.min(professores$gpa), ]  # mais difícil
professores[which.max(professores$gpa), ]  # mais fácil
```

```{r}
#Disciplinas

disciplinas <- dbGetQuery(conn, "
    SELECT courses.name,
           SUM(4*a_count + 3.5*ab_count + 3*b_count + 2.5*bc_count + 2*c_count 
           + 1*d_count + 0*f_count) AS pontos,
           SUM(a_count+ab_count+b_count+bc_count+c_count+d_count+f_count) 
           AS alunos
    FROM grade_distributions
    INNER JOIN course_offerings ON course_offerings.uuid 
    = grade_distributions.course_offering_uuid
    INNER JOIN courses ON courses.uuid = course_offerings.course_uuid
    INNER JOIN subject_memberships ON subject_memberships.course_offering_uuid 
    = grade_distributions.course_offering_uuid
    INNER JOIN subjects ON subjects.code = subject_memberships.subject_code
    WHERE subjects.abbreviation = 'STAT'
    GROUP BY courses.name
")
disciplinas$gpa <- disciplinas$pontos / disciplinas$alunos

disciplinas[which.min(disciplinas$gpa), ]  # mais difícil
disciplinas[which.max(disciplinas$gpa), ]  # mais fácil
```

4.  Desconecte do banco de dados.

```{r}
dbDisconnect(conn)
```

```{r}
Sys.setenv(TZ = "America/Sao_Paulo")
Sys.time()
```
